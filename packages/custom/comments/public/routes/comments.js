(function() {
    'use strict';

    function Comments($stateProvider) {
        
    }

    angular
        .module('mean.comments')
        .config(Comments);

    Comments.$inject = ['$stateProvider'];

})();
